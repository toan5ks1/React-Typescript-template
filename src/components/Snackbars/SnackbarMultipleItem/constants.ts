export enum CloseReason {
    TIMEOUT = "timeout",
    MAXSNACK = "maxsnack",
    INSTRUCTED = "instructed",
}

export enum DefaultsSnackbar {
    MAX_SNACK = 3,
    VARIANT = "success",
    AUTO_HIDE_DURATION = 5000,
}
