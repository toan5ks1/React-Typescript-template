import featureControllerUnleash from "src/internals/feature-controller";

function useFeatureController() {
    return { featureController: featureControllerUnleash };
}

export default useFeatureController;
