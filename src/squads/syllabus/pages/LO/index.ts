import { Entities } from "src/common/constants/enum";

import { IAppResource } from "../../models/resource";

const LO: IAppResource = {
    name: Entities.LOS,
    key: Entities.LOS,
};

export default LO;
