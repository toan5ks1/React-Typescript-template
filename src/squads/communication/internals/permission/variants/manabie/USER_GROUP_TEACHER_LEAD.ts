import { defineAbility } from "src/squads/communication/internals/permission/rules";

const rules = defineAbility((_can) => {
    // TODO: Notification with enable specific for teacher https://manabie.atlassian.net/browse/LT-14272 discussion 18
});

export default rules;
